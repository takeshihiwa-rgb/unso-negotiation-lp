# Web（LP）

V0 から同期した Next.js + shadcn 系の LP。**文言の正本**は [`../docs/lp-copy.md`](../docs/lp-copy.md)。

## セットアップ

```bash
cd /Users/takeshihiwa/projects/unso-negotiation-lp/web
pnpm install
pnpm dev
```

`node_modules` が無い場合は上記 `pnpm install` が必須です。

## 同期元

- 直近の V0 エクスポート: `Downloads/b_42SurvOqfNF-1774088301406`（2025-03-21 頃）
